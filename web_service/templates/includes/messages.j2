{% if messages %}
  <div class="messages" style="margin-bottom: 50px">
    {% for message in messages %}
      <div style="font-weight: bold" {% if message.tags %} class="{{ message.tags }}"{% endif %}>{{ message }}</div>
    {% endfor %}
  </div>
{% endif %}
